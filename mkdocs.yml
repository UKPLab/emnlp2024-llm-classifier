site_name: llm_classifier
nav:
  - Home: index.md

theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      project_name: llm_classifier
      handlers:
        python:
          options:
            docstring_style: numpy